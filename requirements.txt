# Core dependencies for Summeets
# Synchronized with pyproject.toml - use conservative version ranges

# CLI and configuration
typer>=0.12,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0.1,<2.0

# LLM providers
openai>=1.40.0,<2.0
anthropic>=0.34.1,<1.0

# UI frameworks
rich>=13.7,<14.0
ttkbootstrap>=1.10.1

# Audio transcription
replicate>=0.25.0,<1.0

# Utilities
tenacity>=8.3.0,<9.0
alive-progress>=3.1.5,<4.0
